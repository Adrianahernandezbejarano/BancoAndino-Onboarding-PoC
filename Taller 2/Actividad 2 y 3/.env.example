# Puerto del servidor
PORT=3002

# Sal para tokens deterministas (no reversible)
# Genera un string aleatorio seguro
TOKEN_SALT=tu-salt-seguro-aqui

# Clave AES-256-GCM en hex (64 caracteres hexadecimales)
# Genera una usando: [System.Security.Cryptography.RandomNumberGenerator] en PowerShell
# o usa: openssl rand -hex 32
DATA_KEY_HEX=genera-tu-clave-segura-de-64-caracteres-hexadecimales-aqui

# SelecciÃ³n de backend del vault: sqlite | mongo
VAULT_BACKEND=sqlite

# Ruta del archivo de base de datos SQLite (opcional, por defecto vault.db)
VAULT_DB_PATH=vault.db

# MongoDB (solo si VAULT_BACKEND=mongo)
# MONGODB_URI=mongodb+srv://usuario:password@cluster0.xxxxx.mongodb.net/?appName=Cluster0
# MONGODB_DB=pii_vault

# OpenAI (solo si quieres usar /secureChatGPT)
# OPENAI_API_KEY=sk-tu-clave-de-openai-aqui
# OPENAI_MODEL=gpt-4o-mini
